/**
 * Configurações do frontend que dependem do ambiente.
 * O Client ID do Google é carregado em tempo de execução por /api/config.
 * Configure-o somente no arquivo .env do servidor.
 */
window.FINANHUB_CONFIG = {
  API_BASE_URL: 'http://localhost:3000/api',
};
